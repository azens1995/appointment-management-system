// Write your tests here
describe('Name of the group', () => {
  test('Your first unit test', () => {
    // Arrange
    const defaultValue = 2;
    // Act
    // Assert
    expect(2).toBe(defaultValue);
  });
});
