/* Note: 
    No need to test this function.
    While testing function of ch-5.ts (calculateProfit), return value of this function (calculateProfitPerYear) should not be 23.
    you have to mock this function and let say, it should return 30.
*/
export const calculateProfitPerYear = () => {
  const temp_value = 23;

  return temp_value;
};
